pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls'
                sh './build.sh'
            }
        }

        stage('Test') {
            steps {
                sh 'git --version'
            }
        }
    }
}
