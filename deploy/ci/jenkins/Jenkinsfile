pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'git --version'
            }
        }

        stage('Test') {
            steps {
                sh 'git --version'
            }
        }
    }
}
