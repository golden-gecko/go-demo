apiVersion: v1
kind: PersistentVolume
metadata:
  name: roach-certs-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: nfs.com.gecko
    path: "/roach-certs"
