apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: influx-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  volumeName: influx-volume
