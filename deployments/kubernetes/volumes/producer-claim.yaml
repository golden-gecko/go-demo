apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: producer-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
  volumeName: producer-volume
