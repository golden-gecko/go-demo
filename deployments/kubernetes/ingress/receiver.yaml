apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: receiver
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
    - hosts:
      - receiver.com.gecko
      secretName: stack-tls
  rules:
    - host: receiver.com.gecko
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: receiver
                port:
                  number: 7000
